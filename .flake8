# flake8 doesn't read pyproject.toml, so its config lives here.
[flake8]
max-line-length = 88
# E203 (whitespace before ':') and W503 (line break before binary operator)
# disagree with black's formatting; defer to black on both.
extend-ignore = E203, W503
exclude = .git, __pycache__
