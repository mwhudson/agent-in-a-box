<!--
This file is bind-mounted into lxd-claude session containers at
~/.claude/CLAUDE.md, so it serves as the global instructions for every Claude
Code session run via `lxd-claude` (the Claude API container; not the --or /
OpenRouter one).

Edit it here or from inside a container — it's the same file. Add your own
global instructions below.
-->
